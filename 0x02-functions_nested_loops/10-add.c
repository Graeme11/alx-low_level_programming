#include "main.h"

/**
 * add - add two variables
 * @i: The first variable to be added
 * @l: The Second variable to be added
 * Return: the sum of the two variables
 */
int add(int i, int l)
{
	int n;
	 n = i + l;
	return (n);
}
