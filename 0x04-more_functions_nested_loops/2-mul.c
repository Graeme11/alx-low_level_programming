#include "main.h"

/**
 * mul - checks for lowercase character.
 * @a: entire receipt for main.
 * @b: integer received by main
 * Return: 1 if c is uppercase
 */
int mul(int a, int b)
{
	return (a * b);
}
